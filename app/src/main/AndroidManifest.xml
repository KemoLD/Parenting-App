<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmpt276.teal.parentingpro">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HistoryActivity"></activity>
        <activity android:name=".FlipCoinActivity" />
        <activity android:name=".ConfigActivity"></activity>
        <activity android:name=".TimerActivity" />
>>>>>>>>> Temporary merge branch 2
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".TimeService"/>
        <receiver android:name=".CountDownReceiver">
            <intent-filter>
                <action android:name="com.cmpt276.teal.parentingpro.COUNTDOWN_INNER" />
                <action android:name="com.cmpt276.teal.parentingpro.COUNTDOWN_END" />
            </intent-filter>
        </receiver>
    </application>

</manifest>